plugins {
    id("java")
}


repositories {
    mavenCentral()
}

dependencies {
    testImplementation("junit:junit:4.13")
}

test {
    useJUnit()
    ignoreFailures = false
}

tasks.withType(Test).configureEach {
    // Example: pass a property "failTest"
    systemProperty("failTest", project.findProperty("failTest") ?: "true")
}
